.npmrc generated to help pnpm hoist and fetch platform binaries
shamefully-hoist=true
public-hoist-pattern[]=*

# Instruct some tools to fetch additional prebuilt binaries for these platforms
# (helps prebuild-install / package installers fetch cross-platform binaries)
binary-platforms=linux-x64,darwin-arm64

# Allow scripts to run with elevated permissions during install when needed
unsafe-perm=true

# Reduce transient network failures
fetch-retries=3
fetch-retry-factor=2
fetch-retry-mintimeout=10000